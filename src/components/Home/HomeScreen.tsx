import React from 'react';
import { View, Text } from 'react-native';

const HomeScreen = () => {
    return (
        <View>
            <Text>최근에 등록된 구매/판매글</Text>
            {/* 이곳에 구매 및 판매글을 리스트 형태로 표시 */}
        </View>
    );
};

export default HomeScreen;
